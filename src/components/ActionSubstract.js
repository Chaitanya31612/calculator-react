import React from 'react';

const ActionSubstract = (props) => (
    <div className="container">
      <button className="button" onClick={props.handleSubstract}>Substract</button>
    </div>
);

export default ActionSubstract;